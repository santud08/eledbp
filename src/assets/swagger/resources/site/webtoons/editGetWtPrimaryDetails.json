{
    "title_id":{
        "name": "title_id",
        "in":"query",
        "required":true,
        "description": "title_id for which details you want",
        "schema": {
          "type": "integer",
          "default": ""
        }
    },
    "request_id":{
        "name": "request_id",
        "in":"query",
        "required":false,
        "description": "request_id for which details you want",
        "schema": {
          "type": "integer",
          "default": ""
        }
    },
    "site_language":{
        "name": "site_language",
        "in":"query",
        "required":true,
        "description": "site_language for which details you want",
        "schema": {
          "type": "string",
          "example":"en/ko",
          "default": "en"
        }
    }
}