{
    "search_text":{
        "name": "search_text",
        "in":"query",
        "required":true,
        "description": "search text",
        "schema": {
          "type": "string",
          "default": ""
        }
    },
    "page":{
        "name": "page",
        "in":"query",
        "required":false,
        "description": "page",
        "schema": {
          "type": "integer",
          "default": 1
        }
    },
    "limit":{
        "name": "limit",
        "in":"query",
        "required":false,
        "description": "limit",
        "schema": {
          "type": "integer",
          "default": 10
        }
    }
}