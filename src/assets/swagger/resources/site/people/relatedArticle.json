{
    "person_id":{
        "name": "person_id",
        "in":"query",
        "required":true,
        "description": "people id  which details you want",
        "schema": {
          "type": "integer",
          "example":1
        }
    },
    "page":{
        "name": "page",
        "in":"query",
        "required":false,
        "description": "no of page",
        "schema": {
          "type": "integer",
          "example":1
        }
    },
    "limit":{
        "name": "limit",
        "in":"query",
        "required":false,
        "description": "no of limit",
        "schema": {
          "type": "integer",
          "example":3
        }
    }
}